// Build marker — bumped to force a fresh CI build / deployment.
// Increment BUILD_ID (or update BUILT_AT) to trigger a new APK + web build.
export const BUILD_ID = 2
export const BUILT_AT = "2026-06-01T23:40:00Z"
